(ns io-orkes.clojure-sdk-examples-test
  (:require [clojure.test :refer :all]
            [io-orkes.clojure-sdk-examples :refer :all]))

(deftest a-test
  (testing "FIXME, I fail."
    (is (= 0 1))))
